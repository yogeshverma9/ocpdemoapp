--- a/01-imagestream.yaml
+++ /dev/null
@@ -1,36 +0,0 @@
-apiVersion: image.openshift.io/v1
-kind: ImageStream
-metadata:
-  name: hello-openshift
-  namespace: migration-test
-  labels:
-    app: hello-ocp
-spec:
-  lookupPolicy:
-    local: false
-  tags:
-    - name: latest
-      from:
-        kind: DockerImage
-        name: docker.io/openshift/hello-openshift:latest
-      importPolicy:
-        scheduled: true
-      referencePolicy:
-        type: Source
----
-apiVersion: image.openshift.io/v1
-kind: ImageStream
-metadata:
-  name: hello-nodejs
-  namespace: migration-test
-  labels:
-    app: hello-ocp
-spec:
-  lookupPolicy:
-    local: false
-# NOTE:
-# ImageStream is an OpenShift-only resource with no direct AKS/K8s
-# equivalent. On migration this typically becomes:
-#   - an image reference pulled from Azure Container Registry (ACR), and
-#   - a CI/CD pipeline step (GitHub Actions / Azure Pipelines) replacing
-#     the ImageChange trigger behavior seen in DeploymentConfig below.
--- a/02-buildconfig.yaml
+++ /dev/null
@@ -1,34 +0,0 @@
-apiVersion: build.openshift.io/v1
-kind: BuildConfig
-metadata:
-  name: hello-nodejs-build
-  namespace: migration-test
-  labels:
-    app: hello-ocp
-spec:
-  source:
-    type: Git
-    git:
-      uri: https://github.com/sclorg/nodejs-ex.git
-      ref: master
-  strategy:
-    type: Source
-    sourceStrategy:
-      from:
-        kind: ImageStreamTag
-        namespace: openshift
-        name: 'nodejs:18-ubi8'
-  output:
-    to:
-      kind: ImageStreamTag
-      name: 'hello-nodejs:latest'
-  triggers:
-    - type: ConfigChange
-    - type: ImageChange
-# NOTE:
-# BuildConfig (S2I builds) is OpenShift-only. On AKS this build step is
-# usually replaced with a CI pipeline (Azure Pipelines / GitHub Actions)
-# that builds a container image with Docker/Buildpacks and pushes it to
-# Azure Container Registry (ACR). This file references the standard
-# public "nodejs-ex" sample repo, purely so the BuildConfig kind has a
-# realistic, working example for your conversion tooling to parse.
--- a/05-deploymentconfig.yaml
+++ b/05-deploymentconfig.yaml
@@ -1,5 +1,5 @@
-apiVersion: apps.openshift.io/v1
-kind: DeploymentConfig
+apiVersion: apps/v1
+kind: Deployment
 metadata:
   name: hello-openshift
   namespace: migration-test
@@ -8,11 +8,12 @@
 spec:
   replicas: 2
   selector:
-    app: hello-ocp
-    deploymentconfig: hello-openshift
+    matchLabels:
+      app: hello-ocp
+      deploymentconfig: hello-openshift
   strategy:
-    type: Rolling
-    rollingParams:
+    type: RollingUpdate
+    rollingUpdate:
       maxSurge: 25%
       maxUnavailable: 25%
   template:
@@ -41,18 +42,18 @@
             limits:
               cpu: 200m
               memory: 128Mi
+          livenessProbe:
+            httpGet:
+              path: /
+              port: 8080
+            initialDelaySeconds: 10
+            periodSeconds: 15
           readinessProbe:
             httpGet:
               path: /
               port: 8080
             initialDelaySeconds: 5
             periodSeconds: 10
-          livenessProbe:
-            httpGet:
-              path: /
-              port: 8080
-            initialDelaySeconds: 10
-            periodSeconds: 15
           volumeMounts:
             - name: hello-data
               mountPath: /data
@@ -60,20 +61,10 @@
         - name: hello-data
           persistentVolumeClaim:
             claimName: hello-pvc
-  triggers:
-    - type: ConfigChange
-    - type: ImageChange
-      imageChangeParams:
-        automatic: true
-        containerNames:
-          - hello-openshift
-        from:
-          kind: ImageStreamTag
-          name: 'hello-openshift:latest'
 # NOTE:
 # DeploymentConfig is OpenShift-only. The AKS/K8s equivalent is a
 # standard "Deployment" (apps/v1). Migration considerations:
 #   - ImageChange/ConfigChange triggers -> handled by CI/CD updating the
 #     image tag and re-applying the Deployment (no built-in trigger).
 #   - rollingParams (maxSurge/maxUnavailable) map directly onto
-#     Deployment's spec.strategy.rollingUpdate fields.
+#     Deployment's spec.strategy.rollingUpdate fields.--- /dev/null
+++ b/07-ingress.yaml
@@ -0,0 +1,25 @@
+apiVersion: networking.k8s.io/v1
+kind: Ingress
+metadata:
+  annotations:
+    haproxy.org/ssl-redirect: "false"
+  labels: app: hello-ocp
+  name: hello-openshift
+  namespace: migration-test
+spec:
+  ingressClassName: haproxy
+  tls:
+    - hosts:
+        - REPLACE_ME_HOST
+      secretName: REPLACE_ME_TLS_SECRET
+  rules:
+    - host: REPLACE_ME_HOST
+      http:
+        paths:
+          - path: /
+            pathType: Prefix
+            backend:
+              service:
+                name: hello-openshift
+                port:
+                  number: 80--- a/07-route.yaml
+++ /dev/null
@@ -1,22 +0,0 @@
-apiVersion: route.openshift.io/v1
-kind: Route
-metadata:
-  name: hello-openshift
-  namespace: migration-test
-  labels:
-    app: hello-ocp
-spec:
-  to:
-    kind: Service
-    name: hello-openshift
-    weight: 100
-  port:
-    targetPort: http
-  tls:
-    termination: edge
-    insecureEdgeTerminationPolicy: Redirect
-# NOTE:
-# Route is OpenShift-only. On AKS this maps to a Kubernetes "Ingress"
-# resource (e.g. via NGINX Ingress Controller or the Azure Application
-# Gateway Ingress Controller / AGIC), or to Azure Front Door / App
-# Gateway for TLS-terminated public entry points.
--- a/08-hpa.yaml
+++ b/08-hpa.yaml
@@ -7,8 +7,8 @@
     app: hello-ocp
 spec:
   scaleTargetRef:
-    apiVersion: apps.openshift.io/v1
-    kind: DeploymentConfig
+    apiVersion: apps/v1
+    kind: Deployment
     name: hello-openshift
   minReplicas: 1
   maxReplicas: 3
