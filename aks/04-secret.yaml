apiVersion: v1
kind: Secret
metadata:
  name: hello-secret
  namespace: migration-test
  labels:
    app: hello-ocp
type: Opaque
stringData:
  API_KEY: "test-api-key-change-me"
  DB_PASSWORD: "test-password-change-me"
# NOTE:
# These are dummy test values only - never commit real secrets to Git.
# On AKS, secrets are commonly backed by Azure Key Vault via the
# Secrets Store CSI Driver instead of plain Kubernetes Secrets.
