apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-config
  namespace: migration-test
  labels:
    app: hello-ocp
data:
  APP_MESSAGE: "Hello from OCP - migration pipeline test"
  APP_ENV: "test"
  RESPONSE_DELAY_MS: "0"
